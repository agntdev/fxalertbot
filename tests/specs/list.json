[
  {
    "name": "/list shows empty state when no alerts exist",
    "steps": [
      {
        "send": { "text": "/list" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "No alerts yet — tap ➕ Add alert to create one." } }
        ]
      }
    ]
  }
]
